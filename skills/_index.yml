generated: '2026-08-12'
method: searched
source: https://finesse.us/agents.md
note: >-
  FINESSE publishes its own agent-facing instruction document at https://finesse.us/agents.md and
  advertises it through a dedicated agentic-discovery sitemap
  (https://finesse.us/sitemap_agentic_discovery.xml). That document is saved verbatim below. The
  two packaged skills are generated by API Evangelist and are grounded exclusively in the live,
  unauthenticated MCP tools/list manifest and the read-only endpoints the store documents itself —
  no tool, argument or endpoint was invented.
skills:
  - file: finesse-agents.md
    name: FINESSE agent instructions (provider-published)
    method: searched
    source: https://finesse.us/agents.md
    http_status: 200
    content_type: text/markdown
    description: >-
      The store's canonical agent-facing description: UCP discovery and MCP endpoint, the typical
      agent flow, the buyer-approval invariant, rate-limit guidance, the read-only browsing surface,
      store policies, and a recommendation to install the cross-store Shop skill. Mirrored at
      https://finesse.us/llms.txt.
  - file: finesse-agentic-checkout.md
    name: Agentic checkout at FINESSE
    method: generated
    source: mcp/finesse-mcp-tools-list.json
    api: FINESSE UCP Commerce MCP
    operations:
      - search_catalog
      - create_cart
      - create_checkout
      - update_checkout
      - complete_checkout
      - get_order
    description: >-
      End-to-end buy-for-me flow over the UCP/MCP endpoint, including the required
      meta.ucp-agent.profile argument, minor-unit money handling, single-destination fulfillment,
      and the contemporaneous buyer-approval rule.
  - file: finesse-catalog-browse.md
    name: Reading the FINESSE catalog
    method: generated
    source: https://finesse.us/agents.md
    api: FINESSE Storefront JSON (read-only)
    operations:
      - GET /products.json
      - GET /products/{handle}.json
      - GET /collections/{handle}/products.json
      - GET /search?q={query}&type=product
    description: >-
      Non-transacting read of the product catalog over the unauthenticated storefront JSON
      endpoints, with the observed response shape and the robots.txt boundaries.
